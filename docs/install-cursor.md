# Install For Cursor

1. Copy `adapters/cursor/AGENTS.md` and the adjacent `references/` and `scripts/` directories into the project root used by Cursor.
2. Preserve the file name `AGENTS.md`.
3. Reload the workspace or restart Cursor before using the adapter.
