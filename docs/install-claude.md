# Install For Claude

1. Copy `adapters/claude/CLAUDE.md` and the adjacent `references/` and `scripts/` directories into the instruction location you use for Claude.
2. Preserve relative paths so the references and validator remain available beside `CLAUDE.md`.
3. Reload the workspace or restart the Claude session before use.
