# Install For OpenCode

1. Copy `adapters/opencode/AGENTS.md` and the adjacent `references/` directory into the project root used by OpenCode.
2. Preserve the file name `AGENTS.md`.
3. Restart or reload OpenCode before using the adapter.
